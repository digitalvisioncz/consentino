# `@consentino/cookiebot`

Synchronizes Cookiebot statistics consent with Webflow site tracking.

Configure Webflow to track visitors only after opt-in, then place this as the first executable script in `<head>`, before Cookiebot:

```html
<script src="https://cdn.jsdelivr.net/npm/@consentino/cookiebot"></script>
```

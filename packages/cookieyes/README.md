# `@consentino/cookieyes`

Synchronizes CookieYes analytics consent with Webflow site tracking.

Configure Webflow to track visitors only after opt-in, then place this as the first executable script in `<head>`, before CookieYes:

```html
<script src="https://cdn.jsdelivr.net/npm/@consentino/cookieyes@1"></script>
```
